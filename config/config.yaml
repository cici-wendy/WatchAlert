Server:
  port: "9001"
  # release / debug / test
  mode: "release"

Database:
  # 数据库类型: mysql 或 sqlite (默认: mysql)
  type: mysql
  # MySQL 配置 (当 type 为 mysql 时使用)
  host: rm-wz9ww267i81s679kioo.mysql.rds.aliyuncs.com
  port: 3306
  user: watchalert
  pass: Watchalert962464
  dbName: watchalert
  timeout: 10s
  # SQLite 配置 (当 type 为 sqlite 时使用)
  path: data/w8t.db

Redis:
  host: w8t-redis
  port: 6379
  pass: ""
  database: 0

Jwt:
  # 失效时间
  expire: 18000